"""WeekPilot MCP Server package.

Exposes a small set of **public, non-PII** tools (weather + date/time) over the
Model Context Protocol (MCP) using a local stdio transport. See ``server.py`` for
the transport entry point and ``tools_impl.py`` for the (MCP-independent) logic.
"""
